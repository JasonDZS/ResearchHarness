# ResearchHarness

Initial `v0.1` foundation for a session-first research harness.

## Quick start

```bash
python -m researchharness.cli --workspace /path/to/project
python -m researchharness.cli --workspace /path/to/project "Investigate speculative decoding"
python -m unittest discover -s tests -v
```

